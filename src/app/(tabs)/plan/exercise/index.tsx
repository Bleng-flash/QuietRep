import { layout, typography } from '@/styles';
import { Text, View } from 'react-native';

export default function ExerciseListScreen() {
  return (
    <View style={layout.centered}>
      <Text style={typography.body}>All Exercises — coming soon</Text>
    </View>
  );
}
