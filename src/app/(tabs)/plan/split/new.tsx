import { layout, typography } from "@/styles";
import { Text, View } from "react-native";

export default function NewSplitScreen() {
  return (
    <View style={layout.centered}>
      <Text style={typography.body}>New Split — coming soon</Text>
    </View>
  );
}
