import { layout, typography } from "@/styles";
import { Text, View } from "react-native";

export default function SplitDetailScreen() {
  return (
    <View style={layout.centered}>
      <Text style={typography.body}>Split Detail — coming soon</Text>
    </View>
  );
}
